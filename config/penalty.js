/**
 * Penalty Mode Configuration
 * Constants for penalty kick mode
 */

export const PENALTY = {
    // Direction adjustment
    DIRECTION_STEP: 0.02, // Step size for direction adjustment
    
    // Power charging
    POWER_RATE: 2, // Power increase per second
    MAX_POWER: 1, // Maximum power (0-1)
    
    // Shooting
    SHOOT_POWER_MULTIPLIER: 30, // Multiplier for shoot power
    SHOOT_DIRECTION_Y: 0.3, // Y component of shoot direction
    SHOOT_DIRECTION_Z_BASE: -1, // Base Z component of shoot direction
};

